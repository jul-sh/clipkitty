[package]
name = "purr-sync"
version = "0.1.0"
edition = "2021"

[lib]
name = "purr_sync"

[dependencies]
purr-core = { path = "../purr-core" }
uniffi = { version = "*", features = ["cli"] }
uuid = { version = "*", features = ["v4"] }
